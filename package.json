{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "update-ci": "( echo $(date) && php artisan down --refresh=15 --secret=\"letmeenteranywayplz\" && rm -f public/next-maintenance.txt && git restore . && ../narrowblast-update.sh && git submodule sync && git submodule update && git submodule status && composer install --no-interaction --optimize-autoloader && npm install && npm run build && php artisan migrate --force && php artisan optimize ) > storage/logs/last-update-ci-output.log",
        "notify-takedown": "node tools/notify-takedown.js"
    },
    "devDependencies": {
        "@tailwindcss/forms": "^0.5.6",
        "@tailwindcss/typography": "^0.5.10",
        "autoprefixer": "^10.4.16",
        "axios": "^0.25",
        "dotenv": "^16.3.1",
        "laravel-vite-plugin": "^0.8.0",
        "postcss": "^8.4.30",
        "resolve-url-loader": "^3.1.0",
        "sass": "^1.68.0",
        "tailwindcss": "^3.3.3",
        "vite": "^4.4.9"
    },
    "dependencies": {
        "notyf": "^3.10.0",
        "reveal.js": "^4.6.1"
    }
}
